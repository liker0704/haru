assertions:
  - kind: no_zombies
    label: "No zombie agents after mission supersession"
    expected: true

  - kind: max_stall_rate
    label: "Supersession flow does not stall the watchdog"
    expected: 0.0

  - kind: custom
    label: "New mission created with parent_mission_id and original mission superseded"
    expected: true
    hookPath: evals/pr-refactor/assertions/supersession.ts
