{
	"hooks": {
		"SessionStart": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; ha prime --agent \"$HARU_AGENT_NAME\""
					},
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; if ! ha mail check --inject --agent \"$HARU_AGENT_NAME\"; then printf '%s [%s] mail check failed (SessionStart)\\n' \"$(date -Iseconds)\" \"$HARU_AGENT_NAME\" >> \".overstory/logs/hook-errors.log\" 2>/dev/null; fi; exit 0"
					}
				]
			}
		],
		"UserPromptSubmit": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; if ! ha mail check --inject --agent \"$HARU_AGENT_NAME\"; then printf '%s [%s] mail check failed (UserPromptSubmit)\\n' \"$(date -Iseconds)\" \"$HARU_AGENT_NAME\" >> \".overstory/logs/hook-errors.log\" 2>/dev/null; fi; exit 0"
					}
				]
			}
		],
		"PreToolUse": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; ha log tool-start --agent \"$HARU_AGENT_NAME\" --stdin"
					}
				]
			},
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; read -r INPUT; CMD=$(echo \"$INPUT\" | sed 's/.*\"command\": *\"\\([^\"]*\\)\".*/\\1/'); if echo \"$CMD\" | grep -qE '\\bgit\\s+push\\b'; then echo '{\"decision\":\"block\",\"reason\":\"git push is blocked — use ha merge to integrate changes, push manually when ready\"}'; exit 0; fi;"
					}
				]
			}
		],
		"PostToolUse": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; ha log tool-end --agent \"$HARU_AGENT_NAME\" --stdin"
					},
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; if ! ha mail check --inject --agent \"$HARU_AGENT_NAME\" --debounce 5000; then printf '%s [%s] mail check failed (PostToolUse)\\n' \"$(date -Iseconds)\" \"$HARU_AGENT_NAME\" >> \".overstory/logs/hook-errors.log\" 2>/dev/null; fi; exit 0"
					}
				]
			},
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; read -r INPUT; if echo \"$INPUT\" | grep -qE '\\bgit\\s+commit\\b'; then ml diff HEAD~1 >/dev/null 2>&1 || true; fi; exit 0;"
					}
				]
			}
		],
		"Stop": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; ha log session-end --agent \"$HARU_AGENT_NAME\" --stdin"
					},
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; ml learn"
					}
				]
			}
		],
		"PreCompact": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "if [ -z \"$HARU_AGENT_NAME\" ]; then [ -z \"$HARU_RUNTIME_SESSION_ID\" ] && exit 0; [ -f \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\" ] && . \".claude/.agent-env.$HARU_RUNTIME_SESSION_ID\"; [ -z \"$HARU_AGENT_NAME\" ] && exit 0; fi; ha prime --agent \"$HARU_AGENT_NAME\" --compact"
					}
				]
			}
		]
	}
}
